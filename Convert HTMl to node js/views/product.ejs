<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Html To Node</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: #f9f9dc56;
      color: #333;
      padding: 20px;
    }

    header {
      background: #400040;
      color: #F9F9DC;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      margin-bottom: 30px;
    }

    nav a {
      color: #F9F9DC;
      margin-right: 20px;
      text-decoration: none;
      font-size: 18px;
      font-weight: bold;
    }

    nav a:hover {
      text-decoration: underline;
      opacity: 0.8;
    }

    h1 {
      color: #400040;
      margin-bottom: 20px;
      font-size: 36px;
      text-align: center;
    }

    form {

      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 8px 16px #40004077;
      margin-bottom: 50px;
      max-width: 600px;
      margin: 0 auto;
    }

    form input,
    form textarea {
      width: 100%;
      padding: 15px;
      margin-bottom: 15px;
      border: 1px solid #400040;
      border-radius: 6px;
      font-size: 16px;
    }

    form button {
      background-color: #400040;
      color: white;
      border: none;
      padding: 15px 20px;
      cursor: pointer;
      font-size: 18px;
      border-radius: 6px;
      transition: all 0.3s ease;
      width: 100%;
    }

    form button:hover {
      background-color: #400040;
      transform: scale(1.05);
    }

    ul {
      margin-top: 15%;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 2fr));
      gap: 50px;
      list-style: none;
    }

    li {
      background-color: #f9f9dc47;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 8px 16px #400040;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      text-align: center;
    }

    li:hover {
      transform: translateY(-10px);
      box-shadow: 0 12px 24px #400040;
    }

    li h2 {
      margin-bottom: 15px;
      font-size: 24px;
      color: #400040;
    }

    li p {
      margin-bottom: 15px;
      color: #400040;
      font-size: 16px;
    }

    li img {
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      margin-bottom: 20px;
      box-shadow: 0 4px 8px #400040;
    }

    li form button {
      background-color: #400040;
      color: F9F9DC;
      padding: 10px 15px;
      border: none;
      cursor: pointer;
      border-radius: 6px;
      transition: background-color 0.3s ease;
    }

    li form button:hover {
      background-color: #400040;
    }

    .delete-btn {
      padding: 10px 15px;
      background-color: #400040;
      color: #f9f9dc;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
      transition: all 0.3s ease;
      display: block;
      margin: 10px auto 0;
      text-align: center;
    }

    .delete-btn:hover {
      background-color: #6b006b;
      transform: scale(1.05);
    }
  </style>
</head>

<body>
  <%- include('head') %>



    <h1>Add a New Product</h1>
    <form action="/products/add" method="POST">
      <input type="text" name="name" placeholder="Product Name" required />
      <input type="number" name="price" placeholder="Product Price" required />
      <textarea name="description" placeholder="Product Description" required></textarea>
      <input type="text" name="imageUrl" placeholder="Image URL" required />
      <button type="submit">Add Product</button>
    </form>

    <ul>
      <% products.forEach(product=> { %>
        <li>
          <h2>
            <%= product.name %>
          </h2>
          <p>
            <%= product.description %>
          </p>
          <p>Price: $<%= product.price %>
          </p>
          <img src="<%= product.imageUrl %>" alt="<%= product.name %>">
          <a href="/products/edit/<%= product._id %>"></a>
          <form action="/products/delete/<%= product._id %>" method="POST" style="display: inline;padding: 10px 20px;">
            <button type="submit" class="delete-btn">Delete</button>
          </form>
        </li>
        <% }) %>
    </ul>
</body>

</html>